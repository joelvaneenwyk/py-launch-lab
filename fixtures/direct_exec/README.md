# direct_exec

This directory contains helper data for direct launcher scenarios.

Files here are used when testing `.venv` direct execution or raw launcher
invocations that don't fit into the `pkg_*` or `raw_*` categories.

TODO(M2): Populate with scenario-specific helper data.

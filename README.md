# py-launch-lab
Python Launch Lab (`py-launch-lab`) is a Windows-first conformance repo for measuring how Python, PythonW, uv, uvx, uv tool install, venv execution, and a Rust GUI shim actually launch code. It inspects executables, detects console vs GUI behavior, captures evidence, and reports reproducible results across launcher, package, and environment modes.
